<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tapitlac.asistenciasdecursos">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainPage"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".alumnos_agregar"
            android:label="Agregar Alumnos" />
        <activity
            android:name=".alumnos_eliminar"
            android:label="Eliminar Alumnos" />
        <activity
            android:name=".alumnos_lista"
            android:label="Lista de alumnos" />
        <activity
            android:name=".asistencia_agregar"
            android:label="@string/title_activity_asistencia_agregar"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>